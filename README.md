# TTS-Wizard-2-F5-TTS

Этот проект представляет собой связующее звено (мост) между **TTS Voice Wizard** и нейросетью **F5-TTS**, разделяя нагрузку на два разных компьютера: Игровой и Стримерский.

Скрипт перехватывает HTTP-запросы на генерацию речи от Voice Wizard на Игровом ПК, подготавливает текст (переводит цифры в слова с правильным склонением), отправляет по локальной сети запрос к F5-TTS на Стримерский ПК, а затем конвертирует полученное аудио в нужный для Voice Wizard формат (Base64, 16-bit, Mono WAV) и возвращает его.

## 🚀 Особенности
* **Разделение нагрузки:** Игровой ПК занимается только перехватом текста и игрой, а Стримерский ПК рендерит тяжелую нейросеть F5-TTS.
* **Продвинутая обработка русского текста:** Автоматическая конвертация чисел в слова (например, `123` -> `сто двадцать три`) с помощью `num2words`.
* **Умная коррекция рода:** Использование морфологического анализатора `Pymystem3` для правильного склонения числительных "один/одна/одно" и "два/две" в зависимости от следующего за ними существительного.
---

## 🛠 Архитектура

1. **Игровой ПК:** Работает игра, запущен TTS Voice Wizard и этот Python-скрипт (`app.py`).
2. **Стримерский ПК:** Запущена программа Pinokio с локально развернутой нейросетью F5-TTS.
3. **Сеть:** Компьютеры общаются по локальной сети через Gradio API.

---

## ⚙️ Установка и настройка

### Шаг 1: Настройка Стримерского ПК (F5-TTS в Pinokio)
Чтобы Игровой ПК мог отправлять запросы на Стримерский, нужно открыть доступ к F5-TTS в локальной сети.
Чтобы не утруждать себя трудной установкой локальной нейросети, банально установите браузер Pinokio что устанавливает всё в один клик, после чего вбейте в поиск следующую ссылку для установки F5 TTS в один клик: https://github.com/pinokiofactory/e2-f5-tts

1. Откройте настройки F5-TTS в браузере Pinokio.
2. Найдите секцию переменных окружения и установите следующие значения:
   * `PINOKIO_SHARE_LOCAL` = `true`
   * `PINOKIO_SHARE_LOCAL_PORT` = `7860`
3. **Перезапустите** F5-TTS в Pinokio.
4. Узнайте локальный IP-адрес Стримерского ПК (например, `192.168.0.112`).

Не забудьте установить русские модели для генерации голоса выбрав "Custom" и заменив ссылки моделей на следующие:
Model: hf://hotstone228/F5-TTS-Russian/model_last.safetensors
Vocab: hf://hotstone228/F5-TTS-Russian/vocab.txt
Config: {"dim": 1024, "depth": 22, "heads": 16, "ff_mult": 2, "text_dim": 512, "text_mask_padding": false, "conv_layers": 4, "pe_attn_head": 1}

Загрузка моделей займёт какое то время в зависимости от скорости интернета, плюсом первая генерация всегда займёт больше обычного в силу того что модели нужно будет загрузится в память видеокарты.

*(Опционально)* Если Игровой ПК всё ещё не может подключиться, добавьте правило в **Брандмауэр Windows (Windows Firewall)** на Стримерском ПК:
* Разрешить входящие подключения для порта `7860` (протокол TCP).

### Шаг 2: Подготовка Игрового ПК (Python-скрипт)
1. Убедитесь, что установлен **Python 3.10+**.
2. Установите **FFmpeg** (обязательно для работы библиотеки `pydub`):
   * Скачайте FFmpeg, распакуйте и добавьте путь к папке `bin` в системную переменную `PATH`.
3. Установите необходимые Python-библиотеки:
   ```bash
   pip install flask pydub num2words pymystem3 gradio-client
   ```
4. Скачайте скрипт `app.py` и откройте его в любом текстовом редакторе.
5. Настройте переменные в блоке `КОНФИГУРАЦИЯ`:
   ```python
   # IP-адрес Стримерского ПК с запущенным F5-TTS
   F5_TTS_URL = "http://ipV4 адрес второго компа:7860/" (ipV4 можно получить вбив команду ipconfig в консоль на втором компе) 
   
   # Путь к файлу-образцу голоса (должен лежать на Игровом ПК)
   REF_AUDIO_PATH = r"C:\какая то папка\мп3 файл с голосом который вы хотите клонировать.mp3"
   REF_TEXT = "субтитры того мп3 файла чтобы не утруждать второй комп расшифровывать его"
   ```

---

## ▶️ Запуск

1. Сначала убедитесь, что F5-TTS запущен на Стримерском ПК.
2. Запустите скрипт на Игровом ПК
3. Вы должны увидеть в консоли следующее:
   ```text
   Mystem инициализирован успешно.
   Подключение к F5-TTS по адресу http://ipV4 адрес второго компа:7860/...
   Модель установлена. Ответ сервера: ...
   F5-TTS Voice Wizard Bridge Запущен!
   Running on http://127.0.0.1:8124
   ```

---

## 🔌 Настройка TTS Voice Wizard

Чтобы перенаправить генерацию из Voice Wizard в наш скрипт:
1. Откройте TTS Voice Wizard.
2. В настройках Text-to-Speech выберите метод отправки запросов (Locally hosted).

Теперь все запросы на генерацию будут лететь в наш Flask-сервер, обрабатываться, генерироваться на Стримерском ПК и возвращаться обратно в виде звука! 🎧

---
## Стример бот на твиче

в репо есть простой f5wizard.py, и f5wizard TI.py который интегрируется с чатом твича, вам потребуется настроить немного там аккаунт ботика на твиче, чат к которому он будет подключатся, и все такое, но весь ваш текст отправляемый на генерацию будет отправлятся в нужный вам чат в качестве субтитров.

---

## 🐛 Возможные проблемы и их решения

* ❌ **Ошибка: `[WinError 10061] Подключение не установлено, т.к. конечный компьютер отверг запрос на подключение`**
  * **Решение:** Стримерский ПК недоступен по указанному IP или порту. Убедитесь, что вы прописали `PINOKIO_SHARE_LOCAL_PORT = 7860` в Pinokio, перезапустили F5-TTS и брандмауэр Windows не блокирует входящие соединения на порту 7860.
* ❌ **Ошибка: `FileNotFoundError: [WinError 2] Не удается найти указанный файл` при обработке аудио**
  * **Решение:** У вас не установлен FFmpeg или он не добавлен в системную переменную `PATH`. Библиотека `pydub` не может конвертировать звук.

---

весь текст выше был сгенерирован нейросетью с небольшими моими правками, как и вообще этот код, я бы тихо говоря не рекомендовала к использованю F5 TTS даже если у вас два компа, если у вас конечно на втором компьютере не стоит какая нибудь 5070ти чтобы генерировать не за 10+ секунд, а за нужные 2. Для более быстрых и облачных генераций порекомендую другой мой проект https://github.com/alexxa69420/TTS-Wizard-2-Silero-Telegram-Bot
